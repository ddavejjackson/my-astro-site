---
import { categoryStyle } from "../lib/CategoryColors";

interface Props {
    category: string;
    count: number;
}

const { category, count } = Astro.props;
---

<a
    href={`/blog/${category}/posts`}
    class={`group block rounded-lg border ${categoryStyle.border} bg-doom-base1 p-5 transition-colors ${categoryStyle.hover} hover:bg-doom-base2`}
>
    <h3 class={`text-lg font-semibold capitalize ${categoryStyle.text}`}>
        {category}
    </h3>
    <p class="text-sm-text-doom-base6 mt-1">
        {count} post{count === 1 ? "" : "s"}
    </p>
</a>
